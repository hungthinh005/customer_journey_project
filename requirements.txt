# Data
pandas==3.0.2
numpy==1.26.4
openpyxl==3.1.5
kagglehub==0.3.10

# ML & Statistics
scikit-learn==1.4.0
lightgbm==4.6.0
Lifetimes==0.11.3
lifelines==0.30.0

# Recommendation
implicit==0.7.2
gensim==4.4.0

# Add this line to tell pip where to find the CPU versions
--extra-index-url https://download.pytorch.org/whl/cpu

# Deep Learning
torch==2.5.1
torchvision==0.20.1

# Vector Search
faiss-cpu==1.13.2

# API
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# LLM Reranker
openai==2.33.0

# Serving store + config + metrics + agent HTTP client (API container & tests)
SQLAlchemy==1.4.52
psycopg2-binary==2.9.9
pydantic-settings==2.1.0
prometheus-client==0.21.0
requests==2.32.3
tenacity==8.3.0

# NOTE: the AI-agent stack (langgraph, langchain-openai), MLflow, Evidently and
# Airflow run inside the Airflow image and are pinned in requirements-airflow.txt.
# They are intentionally NOT installed here: the agent modules import them lazily
# and they would conflict with this image's pandas/openai/pydantic pins.

# Evaluation & Visualization
matplotlib==3.7.2
seaborn==0.12.2

# Testing & CI
pytest==7.4.3
httpx==0.25.2
ruff==0.15.12

# Utilities
joblib==1.3.2
tqdm==4.67.1
scipy==1.11.3
